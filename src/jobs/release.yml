description: >
  This release the code

executor: node

parameters:
  working-directory:
    type: string
    default: "~/project"

working_directory: << parameters.working-directory >>

steps:
  - checkout
  - release
