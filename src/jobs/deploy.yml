description: >
  Copy files and invalidate the distribution

executor: node

parameters:
  working-directory:
    type: string
    default: "~/project"

working_directory: << parameters.working-directory >>

steps:
  - checkout
  - deploy
