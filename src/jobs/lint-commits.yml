description: >
  This lint commits using conventional commits format

executor: node

parameters:
  working-directory:
    type: string
    default: "~/project"

working_directory: << parameters.working-directory >>

steps:
  - checkout
  - lint-commits
