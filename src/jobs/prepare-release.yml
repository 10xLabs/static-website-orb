description: >
  This create or update the release pull request

executor: node

parameters:
  working-directory:
    type: string
    default: "~/project"

working_directory: << parameters.working-directory >>

steps:
  - checkout
  - prepare-release
